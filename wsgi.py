# -*- coding: utf-8 -*-
"""
Точка входа для production WSGI-сервера (Waitress).

ВАЖНО: Безопасность внутренней сети
====================================
По умолчанию сервер привязан к localhost (127.0.0.1).
Это единственный безопасный режим для внутренней сети БЕЗ аутентификации.

Если нужно открыть доступ для других устройств в сети:
  - Установите парольную защиту (например, через reverse proxy)
  - Измените --host=127.0.0.1 на --host=0.0.0.0
  - НО: все endpoints доступны без аутентификации!

Запуск: waitress-serve --host=127.0.0.1 --port=5000 wsgi:application
"""
from app import create_app

application = create_app()

if __name__ == '__main__':
    application.run()
