# Дорожная карта (Roadmap) проекта `personal_assistant`

> **Дата последнего обновления:** 2026-06-04
> **Текущая версия:** 1.0.7

---

## 📋 Содержание

1. [Текущая структура вкладок](#-текущая-структура-вкладок)
2. [Фаза 1 — Базовая (завершено)](#-фаза-1--базовая-завершено)
3. [Фаза 2 — Конструктор v2 (завершено)](#-фаза-2--конструктор-v2-завершено)
4. [Фаза 2.5 — Аудит архитектуры (завершено)](#-фаза-25--аудит-архитектуры-и-code-quality-завершено)
5. [Фаза 2.6 — Оптимизация загрузки и CSV (завершено)](#-фаза-26--оптимизация-загрузки-данных-и-csv-завершено)
6. [Фаза 3 — Расширение функционала](#-фаза-3--расширение-функционала)
7. [Фаза 4 — Интеграции](#-фаза-4--интеграции)
8. [План улучшений (Фазы 0-6)](#-план-улучшений-фазы-0-6)
9. [Итоговая смета](#-итоговая-смета)
10. [Рекомендуемый порядок (Sprint'ы)](#-рекомендуемый-порядок-sprintы)
11. [Риски и компромиссы](#-риски-и-компромиссы)

---

## 🔄 Текущая структура вкладок

| # | Вкладка | Описание |
|---|---------|----------|
| 1 | **🔄 Конвертер** | Загрузка Excel/CSV + профиль → статистика/скачивание |
| 2 | **📁 Профили** | Создание/редактирование профилей маппинга |
| 3 | **🏢 Справочники** | Подразделения (188) + Статьи расходов (8) |
| 4 | **🔧 Конструктор** | Low-code конструктор сводных таблиц |
| 5 | **💬 DeepSeek** | Чат с DeepSeek AI + OCR |

---

## ✅ Фаза 1 — Базовая (завершено)

- [x] Переименована вкладка "Конвертация" → "Конвертер"
- [x] Добавлена 4-я вкладка "🔧 Конструктор"
- [x] DeepSeek стал 5-й вкладкой
- [x] Создан сервис `constructor.py` — 6 функций
- [x] Создан blueprint `constructor_routes.py` — 7 эндпоинтов
- [x] Фронтенд: загрузка → лист → фильтры → pivot → XLSX
- [x] CSS-спиннеры и блокировка кнопок для async-операций

---

## ✅ Фаза 2 — Конструктор v2 (завершено)

- [x] **Декомпозиция datetime** — авто-определение date-колонок, суб-опции (год/квартал/месяц/день), виртуальные колонки
- [x] **Агрегация "Без изменений"** — опция `value="none"`
- [x] **Высота мультиселектов** — 80px → 200px
- [x] **Сохранение сценариев** — JSON, API save/load/list/delete, UI
- [x] **Улучшение UI колонок** — поиск, кнопки "Все"/"Ни одной"
- [x] **Исправление детекции дат** — number > date > text priority
- [x] **Мульти-агрегация** — несколько функций, два формата вывода

---

## ✅ Фаза 2.5 — Аудит архитектуры и Code Quality (завершено)

- [x] TypedDict для структур данных (`src/types.py`)
- [x] Константы для магических строк
- [x] Импорт `re` на уровень модуля
- [x] Замена `except: pass` на `logger.warning`
- [x] Завершение генератора после ошибки DeepSeek
- [x] Общая функция фильтрации `_apply_filters_to_df()` — 7 типов
- [x] Кэширование DataFrame (cached_df)
- [x] Persist `_temp_files` в JSON
- [x] Усечение истории DeepSeek (MAX_HISTORY_MESSAGES=50)
- [x] Вынос планировщиков в `src/scheduler.py`
- [x] Graceful shutdown через `atexit`
- [x] clean_old_uploads при старте
- [x] Rate limiting на /api/login
- [x] SESSION_COOKIE_SECURE настраиваемый через .env
- [x] Баг-фикс: кнопка "Сбросить" — полная очистка `<option>`

---

## ✅ Фаза 2.6 — Оптимизация загрузки данных и CSV (завершено, v1.0.7)

- [x] **Централизованная `read_file_to_df()`** — единая точка входа для CSV и Excel
- [x] **Оптимизация `load_sheet_data()`** — `nrows=limit+offset` вместо чтения всего файла
- [x] **Поддержка CSV** — виртуальные листы, автоопределение кодировки
- [x] **Обновление всех `pd.read_excel()`** → `read_file_to_df()` в constructor.py, converter.py, routes
- [x] **Фронтенд** — accept-атрибуты `.xlsx, .xls, .csv`
- [x] **API-тесты** — 5 тестов на CSV (все пройдены)

---

## ✅ Фаза 2.7 — Оптимизация SQLite + Column filtering + UI (завершено, v1.0.8)

- [x] **SQLite-кэширование** — новый модуль [`src/utils/sqlite_cache.py`](src/utils/sqlite_cache.py): create/save/query/load/delete
- [x] **Устранение двойного чтения Excel** — `load_sheet()` читает всё ОДИН раз → SQLite + preview из одного DataFrame
- [x] **Фильтрация колонок** — чекбоксы "Отображаемые колонки" реально фильтруют вывод, pivot-обязательные колонки сохраняются принудительно
- [x] **UI: relocation кнопки загрузки** — кнопка "📥 Загрузить" и прогресс-бар перенесены под селект листа (вертикальная вёрстка)
- [x] **Очистка кэша** — при `close_file()` SQLite-кэш удаляется
- [x] **Fallback** — при отсутствии SQLite-кэша код падает на чтение из Excel

---

## 🎯 Фаза 3 — Расширение функционала

- [ ] Графики и визуализация на основе сводных таблиц
- [ ] История загрузок файлов
- [ ] Экспорт сводных таблиц в PDF

---

## 🔗 Фаза 4 — Интеграции

- [ ] Подключение к 1С / SAP / ERP через REST API
- [ ] Планировщик регулярных выгрузок
- [ ] Telegram-бот для получения отчётов

---

# План улучшений — Оценка выполнимости

**Общая оценка: ВЫПОЛНИМО на 95%.**

Проект уже имеет отличную архитектурную базу:
- Чистое разделение на `routes/` (контроллеры) и `services/` (бизнес-логика)
- TypedDict-схемы в `src/types.py`
- Blueprint-регистрация в `app.py`
- Вынесенный планировщик в `src/scheduler.py`
- Сервер-сайд сессии с таймаутами

**Что упрощает реализацию:**

| Предложение | Уже есть в проекте |
|-------------|-------------------|
| `MAX_CONTENT_LENGTH` | ✅ Уже задан 50 MB в `src/config.py` |
| `secure_filename` | ✅ Уже используется в `constructor_routes.py` |
| Экспорт CSV | Частично — API для pivot возвращает JSON |
| Пагинация | Уже есть `limit`/`offset` в `apply_filters()` |

---

## 🛡️ Фаза 0 — Быстрые победы (1-2 дня)

### 0.1 Экспорт сводной в CSV напрямую 🟢 Низкая (0.5 дня)

Добавить параметр `format: 'xlsx' | 'csv'` в POST `/api/constructor/download`.

**Бэкенд:**
```python
if output_format == 'csv':
    import csv, io
    output = io.StringIO()
    writer = csv.DictWriter(output, fieldnames=columns)
    writer.writeheader()
    writer.writerows(pivot_data)
```

**Фронтенд:** Рядом с "Скачать XLSX" добавить "📋 CSV".

### 0.2 Тёмная тема 🟢 Низкая (0.5 дня)

CSS-переменные + переключатель 🌙/☀️ + `localStorage.theme`.

### 0.3 Горячие клавиши 🟢 Низкая (0.25 дня)

- `Ctrl+Enter` — построить сводную
- `Ctrl+S` — сохранить сценарий

### 0.4 Обработка ошибки 413 🟢 Низкая (0.25 дня)

Добавить обработчик `@app.errorhandler(413)` в `app.py`.

**Итого Фаза 0: ~1.5 дня**

---

## 🧪 Фаза 1 — Тестирование и качество кода (3-5 дней)

### 1.1 Юнит-тесты для constructor.py

| Функция | Приоритет |
|---------|-----------|
| `_parse_dates_flexible()` | 🔴 CRITICAL |
| `_infer_column_types()` | 🔴 CRITICAL |
| `decompose_date_column()` | 🟠 HIGH |
| `_apply_filters_to_df()` | 🟠 HIGH |
| `build_pivot_table()` | 🟡 MEDIUM |
| `save_scenario()` / `load_scenario()` | 🟡 MEDIUM |
| `_detect_header_row()` | 🟡 MEDIUM |

### 1.2 Интеграционные тесты API

- `POST /api/constructor/upload` — .xlsx и .xls
- `POST /api/constructor/pivot` — корректный pivot
- `POST /api/constructor/scenario/save` — кириллица
- Regression: datetime в ключах, NaT-фильтрация

### 1.3 Требования для разработки

`requirements-dev.txt` с pytest, black, ruff, mypy.

### 1.4 Docker-контейнеризация

`Dockerfile` + `docker-compose.yml` с Flask + Waitress.

**Итого Фаза 1: ~4 дня**

---

## ⚡ Фаза 2 — Производительность и масштабирование (5-7 дней)

### 2.1 Асинхронная обработка (Celery + Redis)

Для больших файлов (100+ МБ) — асинхронный pivot через Celery.

**Архитектура:**
```
Flask → task_id → Redis → Celery worker → build_pivot_table()
Flask ← GET /api/constructor/task/<id> ← статус
```

### 2.2 Веб-сокеты для прогресса (Flask-SocketIO)

Вместо polling — стриминг прогресса через SocketIO.

### 2.3 Пагинация / виртуальная прокрутка

- Кнопки "◀ Пред." / "След. ▶"
- Индикатор "Строки X-Y из N"
- Выбор размера страницы (25/50/100/200)

**Итого Фаза 2: ~6 дней**

---

## 🧠 Фаза 3 — Новый функционал (7-10 дней)

### 3.1 Вычисляемые поля (Custom columns) 🟠 Высокая (2-3 дня)

Безопасный мини-язык выражений через `pd.eval()` с белым списком:
- `left()`, `right()`, `mid()`, `len()`, `upper()`, `lower()`, `trim()`
- `if(cond, true_val, false_val)`
- `year()`, `month()`, `quarter()`

### 3.2 Экспорт в PDF с графиками 🟠 Высокая (2-3 дня)

- Matplotlib + ReportLab (или WeasyPrint)
- Столбчатая диаграмма по первым строкам

### 3.3 Версионирование сценариев 🟡 Средняя (1 день)

Хранение версий как `scenario_v1.json`, `scenario_v2.json`.

### 3.4 ~~Поддержка CSV~~ ✅ **РЕАЛИЗОВАНО В v1.0.7**

### 3.5 Интеграция с Яндекс.Диск / Google Drive 🟠 Высокая (3-4 дня)

OAuth + скачивание файлов из облака.

**Итого Фаза 3: ~8 дней**

---

## 🔐 Фаза 4 — Безопасность (1-2 дня)

### 4.1 CSRF-защита (Flask-WTF) 🟡 Средняя (1 день)

```python
from flask_wtf.csrf import CSRFProtect
csrf = CSRFProtect()
csrf.init_app(app)
```

### 4.2 Санация имён файлов 🟢 Низкая (0.5 дня)

Проверить все места загрузки на `secure_filename`.

**Итого Фаза 4: ~1.5 дня**

---

## 🎨 Фаза 5 — UI/UX (3-5 дней)

### 5.1 Drag & Drop для загрузки файлов 🟢 Низкая (0.5 дня)

Пунктирная область "📥 Перетащите файл сюда".

### 5.2 (Уже в Фазе 0) Тёмная тема, горячие клавиши, CSV-экспорт

**Итого Фаза 5: ~3 дня**

---

## 📊 Фаза 6 — Мониторинг и отладка (2-3 дня)

### 6.1 Prometheus метрики 🟡 Средняя (1 день)

`prometheus-flask-exporter` — `GET /metrics`.

### 6.2 Дашборд администратора 🟡 Средняя (1.5 дня)

- Список активных `_temp_files`
- Количество сессий
- Статус Celery-воркера
- Кнопки очистки кэша и uploads

**Итого Фаза 6: ~2.5 дня**

---

## 📊 Итоговая смета

| Фаза | Описание | Дней | Приоритет |
|------|----------|------|-----------|
| 0 | Быстрые победы (CSV, тёмная тема, хоткеи) | 1.5 | 🔴 Высокий |
| 1 | Тесты + Docker + линтеры | 4 | 🔴 Высокий |
| 2 | Производительность (Celery + пагинация) | 6 | 🟠 Средний |
| 3 | Новый функционал (формулы, PDF, версии, облака) | 8 | 🟡 Низкий |
| 4 | Безопасность (CSRF) | 1.5 | 🔴 Высокий |
| 5 | UI/UX (Drag&Drop) | 3 | 🟡 Низкий |
| 6 | Мониторинг (Prometheus + админка) | 2.5 | 🟡 Низкий |
| **ИТОГО** | **Всё вместе** | **~27 дней** | |

---

## 🎯 Рекомендуемый порядок (Sprint'ы)

### Sprint 1 — Фундамент (5 дней)
1. **Фаза 0** (1.5 дня) — быстрые победы
2. **Фаза 1** (тесты, Docker) начать параллельно (2-3 дня)
3. **Фаза 4** (CSRF) — 1 день

### Sprint 2 — Производительность (6 дней)
4. **Фаза 2** (Celery + пагинация + WebSocket)

### Sprint 3 — Фичи (8 дней)
5. **Фаза 3** (вычисляемые поля, PDF, версионирование)

### Sprint 4 — Полировка (4 дня)
6. **Фаза 5** (Drag&Drop, тёмная тема)
7. **Фаза 6** (мониторинг, админка)

---

## ⚠️ Риски и компромиссы

| Риск | Вероятность | Смягчение |
|------|-------------|-----------|
| Celery избыточен для текущих нагрузок | 🟡 Средняя | Использовать `threading` или APScheduler как лёгкую альтернативу |
| OAuth для облачных дисков (Яндекс/Google) | 🟠 Высокая | Ограничиться download-by-link, без OAuth |
| PDF с графиками — высокое потребление памяти | 🟡 Средняя | Ограничить топ-10 строк для графика |
| Вычисляемые поля — риск инъекций | 🟠 Высокая | Только `pd.eval()`, белый список функций |
| Поддержка CSV может сломать существующую логику | 🟢 Низкая | Отдельный путь для CSV ✅ (реализовано) |

---

## 💬 Итоговая оценка выполнимости

**Общий вердикт: ВЫПОЛНИМО на 95%**

**Что можно сделать за 1 неделю (реалистично):**

| Что | Дней |
|-----|------|
| ✅ Автотесты (pytest) — constructor + API | 2 |
| ✅ Docker + docker-compose | 1 |
| ✅ CSV-экспорт + тёмная тема + хоткеи | 1 |
| ✅ CSRF-защита (Flask-WTF) | 1 |
| ✅ Drag&Drop + пагинация | 1 |
| **Итого** | **6 дней** |

**Что можно сделать за 2 недели (оптимально):**
- Версионирование сценариев (1 день)
- Prometheus метрики + админка (2 дня)
- Вычисляемые поля (2 дня)

**Что требует больше времени:**
- Celery + WebSocket (3-4 дня)
- PDF с графиками (2-3 дня)
- Облачные интеграции (3-4 дня)
